<view class="container">
  <view class="header">
    <image class="plus" src="../../images/plus.png"/>
    <input class="new-todo"/>
  </view>
  <view class="todos">
    <!-- List items should get the class `completed` when marked as completed -->
    <view class="item{{ item.completed ? ' completed' : '' }}" wx:for="{{ todos }}" wx:key="{{ index }}">
      <!-- completed: success, todo: circle -->
      <icon class="checkbox" type="{{ item.completed ? 'success' : 'circle' }}"/>
      <text class="name">{{ item.name }}</text>
      <icon class="remove" type="clear" size="16"/>
    </view>
  </view>
  <view class="footer">
    <text class="btn">Toggle all</text>
    <text>{{ leftCount }} item left</text>
    <text class="btn">Clear completed</text>
  </view>
</view>
